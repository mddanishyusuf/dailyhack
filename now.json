{
    "version": 2,
    "builds": [{ "src": "package.json", "use": "@now/next" }],
    "routes": [
        { "src": "/post/(?<slug>[a-z-]+-)(?<number>[0-9]+)", "dest": "/post?number=$number&slug=$slug" }
    ]
}